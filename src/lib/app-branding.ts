/** Nombre visible de la aplicación (PNEI / PAI — MSPBS). */
export const APP_TITLE_PRIMARY = 'M R V — PNEI y PAI — MSPBS';

/** Subtítulo corto (cabecera compacta, PWA, etc.). */
export const APP_TITLE_SHORT = 'M R V · PNEI / PAI — MSPBS';

/** Línea de campaña (sigue vigente en terreno). */
export const APP_CAMPAIGN_TAG = 'CVS Sarampión / Rubéola 2026';
